package com.vcs.examples;

public class FloorSearchDao {
	/*
	 * int count = 0; PreparedStatement ps = null; ResultSet rs = null;
	 * Connection connection = null; connection =
	 * ConnectionUtil.getInstance().getConnetion();
	 * 
	 * String searchQuery = WSConstants.GET_FLOOR_COUNT;
	 * 
	 * if (floorId != 0) { searchQuery += WSConstants.WHERE +
	 * WSConstants.FLOOR_ID; } ps =
	 * connection.prepareStatement(searchQuery);
	 * 
	 * if (floorId != 0) { ps.setInt(1, floorId); } rs = ps.executeQuery();
	 * if (rs.next()) { count = rs.getInt("TOTAL_COUNT"); } return count;
	 */
}
